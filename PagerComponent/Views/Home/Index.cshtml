@model PagerComponent.Utilities.PagerResult<Contact>
@{
    ViewData["Title"] = "Home Page";
}

<h2>Entity Framwork Core pager example</h2>
<div class="row">
    <div class="col-md-5">
        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>#</th>
                    <th>First name</th>
                    <th>Last name</th>
                    <th>E-mail</th>
                </tr>
            </thead>
            <tbody>
                @{ var i = 1; }
                @foreach (var contact in Model.Results)
                {
                    var rowNo = (Model.CurrentPage - 1) * Model.PageSize + i;
                    i++;
                    <tr>
                        <td>@rowNo</td>
                        <td>@contact.FirstName</td>
                        <td>@contact.LastName</td>
                        <td>@contact.Email</td>
                    </tr>
                }
            </tbody>
        </table>

        @(await Component.InvokeAsync<PagerComponent.Utilities.PagerViewComponent>(Model))
    </div>
</div>